package com.company;

import org.junit.Test;

import static org.junit.Assert.*;

public class AllKatasTest {

     // private Motorcycle motorcycle = new Motorcycle();
     // private Car car = new Car();


     // @Test
     // public void testInheritance() {

     //     String failMessage = "Expected Motorcycle and Car to both inherit from Vehicle";
     //     assertTrue(failMessage, motorcycle instanceof Vehicle);
     //     assertTrue(failMessage, car instanceof Car);
     // }

     // @Test
     // public void testMethods() throws Exception {

     //     Class moto = Motorcycle.class;
     //     String failMessage = "Expected Motorcycle to have drive method which takes an integer parameter";
     //     assertNotNull(failMessage, moto.getMethod("drive", int.class));
     //     failMessage = "Expected Motorcycle to have displayMilesTraveled method";
     //     assertNotNull(failMessage, moto.getMethod("displayMilesTraveled"));
     //     failMessage = "Expected Motorcycle to have isRunning method";
     //     assertNotNull(failMessage, moto.getMethod("isRunning"));
     //     failMessage = "Expected Motorcycle to have repair method";
     //     assertNotNull(failMessage, moto.getMethod("repair"));

     //     Class car = Car.class;
     //     failMessage = "Expected Car to have drive method which takes an integer parameter";
     //     assertNotNull(failMessage, car.getMethod("drive", int.class));
     //     failMessage = "Expected Car to have displayMilesTraveled method";
     //     assertNotNull(failMessage, car.getMethod("displayMilesTraveled"));
     //     failMessage = "Expected Car to have lockDoors method";
     //     assertNotNull(failMessage, car.getMethod("lockDoors"));
     //     failMessage = "Expected Car to have honk method";
     //     assertNotNull(failMessage, car.getMethod("honk"));

     // }
}
